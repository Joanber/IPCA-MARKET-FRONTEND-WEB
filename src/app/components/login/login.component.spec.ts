import { TestBed, async, ComponentFixture } from '@angular/core/testing';
import { LoginComponen } from './login.component';


describe('Pruebas en el <LoginComponent/>', () => {

    // let componente: LoginComponen;

    // let fixture: ComponentFixture<LoginComponen>;
    
    // beforeEach(async(() => {
    //     TestBed.configureTestingModule({
          
    //       declarations: [ LoginComponen ]
    //     });
    //     fixture = TestBed.createComponent(LoginComponen);
    //     componente = fixture.componentInstance;
    // }));

    // Verifica que la app se crea correctamente
  it('debe de crear componente correctamente', () => {
      console.log("Hola");
  });

});



